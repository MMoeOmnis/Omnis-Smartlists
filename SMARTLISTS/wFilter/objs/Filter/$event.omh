On evClick     ;; Event Parameters - pRow( Itemreference )
	Do iList.$filter(iAge>=iMinAge)     ;; apply filter to smart list
	Do $cwind.$objs.List.$redraw()     ;; redraw list
	
	
On evRMouseDown
	Set reference iObjRef to $cobj